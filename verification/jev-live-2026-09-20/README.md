# Сохранённые живые эксперименты · 20 сентября 2026

- `inspect/`: русский текст, английские вопросы из judge.py; один запрос Jev.
- `first-call-ru/`: русский текст и русские вопросы из first_call.py; один запрос Jev.
- `primary-eval/`: первый замороженный набор, 18 запросов / 28 решений.
- `challenge-eval/`: отдельно подготовленный усложнённый набор, 18 запросов / 28 решений.
- `codex-matrix/`: реальные Jev и Codex, три дефекта, 8 запросов Jev; два завершения и одна остановка.
- `backend-recovery/`: отдельная копия остановленного состояния; обе диагностики и реальный Codex, без нового запроса Jev.

В комплекте 46 запросов Jev из этих запусков. Результаты не объединяются в оценку универсальной точности: первые два запроса демонстрируют примитивы, размеченные наборы проверяют отдельные решения, а matrix проверяет поведение всей программы. Методика и границы выводов — в [VALIDATION.md](../../VALIDATION.md).

Запросы, ответы и журналы сохранены после фактического исполнения. Перед распространением абсолютный путь каталога автора заменён на `<workspace>`, а ссылки состояний на проекты сделаны относительными к упакованной структуре. Значения ответов, измерения, исходы и хеши исполнявшегося кода не подменялись. Ключи и заголовки авторизации не входят в эти файлы.

Клиент дорабатывался между опытами. Поэтому ранние результаты могут не содержать полей `usage_complete` и `usage_reported_requests`, добавленных позднее. Их исходный формат сохранён. Хеши в manifest/protocol относятся к коду конкретного запуска; они не обязаны совпадать с последним состоянием репозитория.

Восстановление не перезаписывает исходную остановку: см. `backend-recovery/protocol.json` и `recovery.json`. Галерея и статья показывают оба этапа отдельно.
